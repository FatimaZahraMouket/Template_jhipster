apiVersion: v1
kind: Pod
metadata:
  labels:
    run: appbiomedicalekube
  name: appbiomedicalekube
spec:
  containers:
  - image: "$DOCKERHUB_USERNAME/bio_app:latest"
    name: appbiomedicalekube
  imagePullSecrets:
    - name : app-secret

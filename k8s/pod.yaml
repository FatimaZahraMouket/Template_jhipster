apiVersion: v1
kind: Pod
metadata:
  labels:
    run: appbiomedicalekube
  name: appbiomedicalekube
spec:
  containers:
  - image: registry.gitlab.com/fatimazahra20mouket/fatimazahrmoukettemplate:$CI_COMMIT_REF_SLUG
    name: appbiomedicalekube
  restartPolicy: Always
  imagePullSecrets:
    - name : app-secret
